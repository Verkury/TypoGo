# TypoGo
Typo is a blind printing console simulator for linux systems. It supports printing in English. Russian will be added later.

## Using
You can just write `typo` in your terminal. And you can write:
```bash
typo filename.txt
```

filename.txt your is your file or path to file. Also you can use not only `txt` files. You can use every format of text file.
